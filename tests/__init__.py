"""This subpackage contains a set of modules for testing the tools in the 
FluvialGeomorph package. 
"""
import sys, os

# Add the parent FluvialGeomorph folder to the system path for all modules
sys.path.insert(0, os.path.abspath('./'))